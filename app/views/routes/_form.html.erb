<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :station1_id %><br />
    <%= f.number_field :station1_id %>
  </div>
  <div class="field">
    <%= f.label :station2_id %><br />
    <%= f.number_field :station2_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
